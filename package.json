{
  "name": "open-transcribe-root",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "install": "cd src && npm install",
    "dev": "cd src && npm run dev",
    "build": "cd src && npm run build",
    "test": "cd src && npm run test",
    "lint": "cd src && npm run lint",
    "check": "cd src && npm run check"
  }
}
